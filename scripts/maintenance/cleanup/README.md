# 清理辅助脚本

- `cleanup_nut_assembly_jobs_dryrun.sh`：盘点旧 NutAssembly / PINN 作业候选，只输出建议，不删除文件。
- `cleanup_pinn_dryrun.sh`：盘点 PhyGen / PINN 实验产物候选，只输出建议，不删除文件。

这两份脚本记录的是历史目录清单。平台运行数据迁移到外部数据根后，输出中的旧路径可能显示为不存在，这是预期现象。
