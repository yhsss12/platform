asset_id: metric_runtime_video_fps_v1
asset_type: metric
name: 视频帧率
version: v1.0.0
status: available
description: 回放 MP4 的编码/播放帧率；优先读取视频 metadata，否则使用录制配置。
metadata:
  displayName: 视频帧率
  implemented: true
  selectable: true
  calculationMode: aggregate_field
  sourceField: runMetrics.videoFps
  valueType: number
  unit: "fps"
  applicableTaskTypes:
    - cable_threading
    - cable_threading_single_arm
    - dual_arm_cable_manipulation
    - block_stacking
    - isaac_block_stacking
    - isaaclab_franka_stack_cube
  applicableEvaluationModes:
    - expert_policy_evaluation
    - trained_model_evaluation
    - episode_stability
    - policy_evaluation
  input_files:
    - results/step_metrics/episode_*/summary.json
    - results/aggregate_result.json
  defaultSelected: false
