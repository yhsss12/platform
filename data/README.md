# Data directory skeleton

This directory mirrors the directory structure of
`/home/ubuntu/project/eai-ide-data` at the time of export.

Only `.gitkeep` placeholders are committed. Dataset files, models, logs,
runtime outputs, caches, and other generated data are intentionally excluded.
The original data remains on the server.
